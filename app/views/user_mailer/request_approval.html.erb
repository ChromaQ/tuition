<h1 style="text-align: center;">Tuition Reimbursement Request</h1>
<% unless Rails.env.production? %>
  <h2 style=" text-align: center; color: #ed8b00"><%= Rails.env.capitalize %> Environment</h2>
<% end %>
<p><strong><%= @user.displayname %></strong> has requested Tuition Reimbursement for </p>

<style>
  body {
    background-color: #eeeeee;
    color: #2f2f2f;
    font-family: helvetica, verdana, arial, sans-serif;
    margin: 1%;
    padding: 1%;
    font-size: 1.3em;
    line-height: 1.5;
  }

  a {
    outline: none;
    padding: 2px 1px 0;
  }

  a:link {
    color: #1f618d;
    font-size: 1.4em;
  }

  a:visited {
    color: #154360;
  }

  a:hover {
    color: #00BFA5;
  }

  p, ol, ul, td li {
    line-height: 1.3;
  }

  h1 {
    font-size: 1.5em;
    color: #007a86;
    line-height: 2em;
    font-family: "American Typewriter", "Source Code Pro", Monaco, "Courier New", serif;
  }

  h3 {
    font-family: "American Typewriter", "Source Code Pro", Monaco, "Courier New", serif;
    font-weight: 500;
  }

  table {
    margin: 0 auto;
    border-collapse: collapse;
    border-style: hidden;
    table-layout: auto;
    width: 100%;
  }

  table td, th {
    padding: 1rem;
    border: 2px solid #BCDEE1;
  }

  th {
    font-family: "American Typewriter", "Source Code Pro", Monaco, "Courier New", serif;
    font-weight: normal;
  }

  .callout-white {
    background-color: white;
    margin: 1em;
    padding: 1em;
    border-radius: 2px;
  }

  .cherry {
    color: #BA0C2F;
  }

  .center-div {
    margin: 0 auto;
    max-width: 700px;
  }

  a.bare:link {
    text-decoration: none;
  }
</style>
