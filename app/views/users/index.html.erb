<ul>
  <% @users.each do |user| %>
    <% if current_user == user %>
      <li><%= user.displayname %> <em class="text-muted">(self)</em></li>
    <% else %>
      <li>Sign in as <%= link_to user.displayname, impersonate_user_path(user), method: :post %></li>
    <% end %>
  <% end %>
</ul>
