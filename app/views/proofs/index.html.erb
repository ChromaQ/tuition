<h1><%= title('Proofs') %></h1>

<table class="table table-hover table-sm">
  <thead>
    <tr>
      <th scope="col">Receipt</th>
      <th scope="col">Grade</th>
      <th scope="col">Course</th>
      <th scope="col">Response</th>
      <th scope="col">Actions</th>
    </tr>
  </thead>

  <tbody>
    <% @proofs.each do |proof| %>
      <tr>
        <td><%= proof.receipt %></td>
        <td><%= proof.grade %></td>
        <td><%= proof.course&.course_title %></td>
        <td><%= proof.response&.capitalize || 'n/a' %></td>
        <td>
          <%= link_to 'Show', proof, class: 'btn btn-sm btn-outline-primary' %>
          <%= link_to 'Edit', edit_proof_path(proof), class: 'btn btn-sm btn-outline-secondary' %>
          <%= link_to 'Destroy', proof, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-sm btn-outline-warning' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<%= link_to 'New Proof', new_proof_path %>
