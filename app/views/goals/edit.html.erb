<div class="container">
  <h1 class="text-center"><%= title('Editing Goal') %></h1>

  <%= render 'form', goal: @goal %>

  <%= link_to 'View', @goal, class: 'btn btn-sm btn-outline-primary' %>
  <%= link_to 'Back', user_path(@goal.user), class: 'btn btn-sm btn-outline-primary' %>
</div>
