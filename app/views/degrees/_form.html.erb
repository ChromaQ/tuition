<%= form_with(model: @degree, local: true) do |f| %>
  <%= errors_for(@degree) %>


    <div class="form-group">
      <%= f.label :name %>
      <%= f.text_field :name, class: 'form-control' %>
    </div>

  <div class="form-group">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>
